# auto-generated by grc.converter

id: variable_uhd_device3
label: Device3
flags:
- show_id

parameters:
-   id: dev_addr
    label: Device Address
    dtype: string
    default: type=x300
    hide: \#if \$dev_addr()
      none
      \#else
      part
      \#end if
-   id: dev_args
    label: Device Arguments
    dtype: string
    default: '""'
    hide: \#if \$dev_addr()
      none
      \#else
      part
      \#end if
value: ${ dev_addr }

templates:
    imports: |-
        from gnuradio import uhd
        import ettus
    var_make: self.device3 = ${id} = ettus.device3(uhd.device_addr_t( ",".join((${dev_addr},
        ${dev_args})) ))

file_format: 1
